# Copiază acest fișier în .env.local și completează cu valorile din
# Supabase Dashboard → Project Settings → API.
#
# Fără aceste variabile, aplicația rulează neschimbată pe date mock
# (comportamentul implicit de azi).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
